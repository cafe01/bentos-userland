/// The entity floor — a repository addressed as an entity, and the arming that
/// makes it react. An application supplies the meaning; this supplies the
/// state, the story, the compare-and-swap, and the emission point.
library;

export 'src/entity/arming.dart';
export 'src/entity/git_entity.dart';
