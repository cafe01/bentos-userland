/// The bentos stdlib — the client-side syscall surface over bentos-kernel
/// capabilities, plus the in-process portal for dev/test.
library;

export 'src/bentos.dart';
export 'src/in_process/in_process_bentos.dart';
