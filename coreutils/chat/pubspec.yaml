name: chat
description: The HumanOS chat coreutil — the turn as a command. One turn against a being's ambient tx session; no REPL (the shell is the REPL).
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.8.0


dependencies:
  args: ^2.4.0
  bentos_userland:
    path: ../..
  chat_inference:
    path: ../../../chat-inference-dart
  tx:
    path: ../tx

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
