name: chatbot
description: chatbot.exe — LUCA with species ontology. The first agent built on the SDK.
version: 0.1.0
publish_to: none


resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  agent_sdk:
    path: ../../../agent-sdk-dart
  args: ^2.4.0
  bentos_userland:
    path: ../..
  chat:
    path: ../chat
  chat_inference:
    path: ../../../chat-inference-dart

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
