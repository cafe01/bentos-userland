name: llm
description: The inert inference coreutil — opens /dev/llm/<vendor>/<model> and streams the answer. Knows no keys, no vendors, no drivers.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  bentos_userland:
    path: ../..

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
