name: chat_render
description: chat-render — reads a ChatEvent JSONL stream and renders it to the terminal.
version: 0.1.0
publish_to: none


resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.4.0
  ansicolor: ^2.0.3
  term_glyph: ^1.2.2
  chat_inference:
    path: ../../../chat-inference-dart

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
