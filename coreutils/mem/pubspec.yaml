name: mem
description: The mem coreutil — the agent's persistent memory organ, as a standalone package.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.8.0

dependencies:
  args: ^2.4.0
  file: ^7.0.0
  path: ^1.9.0
  yaml: ^3.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0
